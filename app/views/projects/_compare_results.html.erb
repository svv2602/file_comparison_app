
<% if results.present? %>
  <h1>Comparison Results</h1>
  <% results.each do |result| %>
    <div>
      <strong>Line:</strong> <%= result[0] %><br>
      <strong>Match:</strong> <%= result[1]["row2"] %>
    </div>
  <% end %>
<% end %>

<%= link_to 'Back', @project %>
